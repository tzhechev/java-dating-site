CREATE TABLE USER_PICTURES
(
  USER_PICTURE_ID NUMBER NOT NULL,
  USER_ID NUMBER NOT NULL,
  USER_PICTURE BLOB NOT NULL
, CONSTRAINT USER_PICTURES_PK PRIMARY KEY
  (
    USER_PICTURE_ID
  )
  ENABLE
)
;

ALTER TABLE USER_PICTURES
ADD CONSTRAINT USER_PICTURES_USERS_FK1 FOREIGN KEY
(
  USER_ID
)
REFERENCES USERS
(
USER_ID
) ENABLE
;
